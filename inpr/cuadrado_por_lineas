/*
    PROPÓSITO:
        * Dibuja un cuadrado de 3x3 de color verde con centro en la celda actual.
    PRECONDICIONES:
        * Hay una celda en cada dirección de la celda actual.
*/
program {
    DibujarCuadradoVerdeDe3x3PorLíneas()
}

/*
    PROPÓSITO:
        * Dibuja un cuadrado de 3x3 de color verde con centro en la celda actual.
    PRECONDICIONES: 
        * Hay una celda en cada dirección de la celda actual.
*/
procedure DibujarCuadradoVerdeDe3x3PorLíneas() {
    IrABaseCuadrado()
    DibujarLíneaVerdeDe3Este()
    IrASiguienteLínea()
    DibujarLíneaVerdeDe3Este()
    IrASiguienteLínea()
    DibujarLíneaVerdeDe3Este()
    VolverAlCentro()
}

/*
    PROPÓSITO:
        * Ubica el cabezal en la esquina suroeste de un cuadrado de 3x3, desde el centro.
    PRECONDICIONES: 
        * Hay una celda al Sur y una celda al Oeste de la celda actual.
*/
procedure IrABaseCuadrado() {
    Mover(Sur)
    Mover(Oeste)
}

/*
    PROPÓSITO:
        * Dibuja una línea de color Verde de longitud 3 hacia el Este.
    PRECONDICIONES: 
        * Hay dos celdas hacia el Este de la celda actual.
*/
procedure DibujarLíneaVerdeDe3Este() {
    Poner(Verde) Mover(Este)
    Poner(Verde) Mover(Este)
    Poner(Verde) VolverLínea()
}

/*
    PROPÓSITO: 
        * Ubica al cabezal en el estremo Oeste de una línea de longitud 3, desde el extremo Este.
    PRECONDICIONES:
        * Hay dos celdas al Oeste de la celda actual.
*/
procedure VolverLínea() {
    Mover(Oeste)
    Mover(Oeste)
}

/*
    PROPÓSITO:
        * Ubicar el cabezal en el extremo Oeste de la siguiente línea.
    PRECONDICIONES: 
        * Hay una celda al Norte de la celda actual.
*/
procedure IrASiguienteLínea() {
    Mover(Norte)
}

/*
    PROPÓSITO:
        * Ubica el cabezal en el centro del cuadrado de 3x3, desde la esquina noroeste.
    PRECONDICIONES: 
        * Hay una celda al Sur y una al Este de la celda actual.
*/
procedure VolverAlCentro() {
    Mover(Sur)
    Mover(Este)
}