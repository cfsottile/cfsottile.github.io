/*
    PROPÓSITO:
        * Dibuja un cuadrado de tamano 3 de color verde con centro en la celda actual.
    PRECONDICIONES: 
        * Hay una celda en cada dirección de la celda actual.
*/
program {
    DibujarCuadradoVerdeDe3x3PorLados()
}

/*
    PROPÓSITO:
        * Dibuja un cuadrado de tamano 3 de color verde con centro en la celda actual.
    PRECONDICIONES:
        * Hay una celda en cada dirección de la celda actual.
*/
procedure DibujarCuadradoVerdeDe3x3PorLados() {
    DibujarPuntoVerde()
    IrABaseCuadrado()
    DibujarLíneaVerdeDe2Norte()
    DibujarLíneaVerdeDe2Este()
    DibujarLíneaVerdeDe2Sur()
    DibujarLíneaVerdeDe2Oeste()
    VolverAlCentro()
}

/*
    PROPÓSITO: Dibuja un punto Verde.
*/
procedure DibujarPuntoVerde() {
    Poner(Verde)
}

/*
    PROPÓSITO:
        * Ubica el cabezal en la esquina suroeste de un cuadrado de 3x3, desde el centro.
    PRECONDICIONES:
        * Hay una celda al Sur y una celda al Oeste de la celda actual.
*/
procedure IrABaseCuadrado() {
    Mover(Sur)
    Mover(Oeste)
}

/*
    PROPÓSITO: 
        * Dibuja una línea de color Verde de longitud dos hacia el Norte.
        * Deja el cabezal dos celdas hacia el Norte de la celda actual.
    PRECONDICIONES:
        * Hay dos celdas al Norte.
*/
procedure DibujarLíneaVerdeDe2Norte() {
    Poner(Verde)
    Mover(Norte)
    Poner(Verde)
    Mover(Norte)
}

/*
    PROPÓSITO: 
        * Dibuja una línea de color Verde de longitud dos hacia el Este.
        * Deja el cabezal dos celdas hacia el Este de la celda actual.
    PRECONDICIONES:
        * Hay dos celdas al Este.
*/
procedure DibujarLíneaVerdeDe2Este() {
    Poner(Verde)
    Mover(Este)
    Poner(Verde)
    Mover(Este)
}

/*
    PROPÓSITO: 
        * Dibuja una línea de color Verde de longitud dos hacia el Sur.
        * Deja el cabezal dos celdas hacia el Sur de la celda actual.
    PRECONDICIONES:
        * Hay dos celdas al Sur.
*/
procedure DibujarLíneaVerdeDe2Sur() {
    Poner(Verde)
    Mover(Sur)
    Poner(Verde)
    Mover(Sur)
}

/*
    PROPÓSITO: 
        * Dibuja una línea de color Verde de longitud dos hacia el Oeste.
        * Deja el cabezal dos celdas hacia el Oeste de la celda actual.
    PRECONDICIONES:
        * Hay dos celdas al Oeste
*/
procedure DibujarLíneaVerdeDe2Oeste() {
    Poner(Verde)
    Mover(Oeste)
    Poner(Verde)
    Mover(Oeste)
}

/*
    PROPÓSITO: 
        * Ubica el cabezal en el centro del cuadrado de 3x3, desde la esquina suroeste.
    PRECONDICIONES:
        * Hay una celda al Norte y una celda al Este de la celda actual.
*/
procedure VolverAlCentro() {
    Mover(Norte)
    Mover(Este)
}